<?xml version="1.0" encoding="utf-8"?>
<!--
  Light-theme fallback background for the "Hijri Date" widget: a flat
  light card with a hairline border, matching the app's light theme
  (AppColors.white / AppColors.border). Only visible before the
  Flutter-rendered PNG lands; once it does, the PNG covers it.

  The dark-theme variant lives in res/drawable-night/ and the system
  picks between them automatically.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <solid android:color="#FFFFFF" />
    <stroke android:width="1dp" android:color="#E8E4DC" />
</shape>
