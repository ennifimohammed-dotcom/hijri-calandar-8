<?xml version="1.0" encoding="utf-8"?>
<!--
  Dark-theme fallback background for the "Hijri Date" widget: a flat
  dark card with a hairline border, matching the app's dark theme
  (AppColors.darkSurface / AppColors.darkBorder). Only visible before
  the Flutter-rendered PNG lands; once it does, the PNG covers it.

  The light-theme variant lives in res/drawable/ and the system picks
  between them automatically.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <solid android:color="#1A2235" />
    <stroke android:width="1dp" android:color="#2A3448" />
</shape>
